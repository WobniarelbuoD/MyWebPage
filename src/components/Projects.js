import { Box, Typography } from '@mui/material';
import React from 'react';

const Projects = () => {
    return (
        <Box minHeight={"100vh"}>
            <Typography align="center" sx={{paddingTop:"10vh"}} variant="h1">a big list of projects</Typography>
        </Box>
    );
};

export default Projects;